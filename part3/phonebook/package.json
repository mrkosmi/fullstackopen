{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Marcel Kosmala",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:front": "rm -rf dist && cd ../../part2/phonebook && npm run build && cp -r dist ../../part3/phonebook",
    "deploy:full": "npm run build:front && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^18.0.3",
    "express": "^5.2.1",
    "mongoose": "^9.10.2",
    "morgan": "^1.12.1"
  }
}
