The application can be accessed online here: **https://fullstackopen-bekg.onrender.com**.

To run the project locally:
1. Clone the repo.
2. Install dependecies using `npm install`.
3. Start the dev server using `npm run dev`.